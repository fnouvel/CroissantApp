---
phase: 01-auth-and-database-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Full auth flow works in a real browser: register, login, use app, refresh page, still logged in, logout"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete auth flow works end-to-end in a real browser.

Purpose: Backend tests confirm API correctness, but browser-specific behavior (HttpOnly cookies, CORS, session persistence across page refresh) can only be verified manually. This checkpoint confirms everything works before closing Phase 1.

Output: Human confirmation that auth flow is complete and correct.
</objective>

<execution_context>
@/Users/samuelclark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samuelclark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-and-database-foundation/01-01-SUMMARY.md
@.planning/phases/01-auth-and-database-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev servers and run full test suite</name>
  <files></files>
  <action>
    1. Run the full backend test suite to confirm everything still passes:
       ```bash
       cd backend && source venv/bin/activate && pytest -q
       ```

    2. Start the backend dev server (if not already running):
       ```bash
       cd backend && source venv/bin/activate && uvicorn app.main:app --reload
       ```

    3. Start the frontend dev server (if not already running):
       ```bash
       cd frontend && npm run dev
       ```

    4. Confirm both servers are accessible by checking:
       - Backend health: curl http://localhost:8000/api/health returns {"status": "ok"}
       - Frontend: curl http://localhost:5173 returns HTML
  </action>
  <verify>
    <automated>cd /Users/samuelclark/Desktop/florecroissant/CroissantApp/backend && source venv/bin/activate && pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>All backend tests pass. Dev servers are running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full auth flow in browser</name>
  <files></files>
  <action>
    Present the following verification steps to the user. This is a human-verify checkpoint — Claude starts dev servers and then pauses for user confirmation.

    What was built: Complete authentication system — user registration, login with JWT tokens, session persistence via HttpOnly refresh cookie, logout, and route protection. Backend auth endpoints + frontend login form + AuthContext + token injection into all API calls.
  </action>
  <verify>
    Human confirms the following steps work:
    1. Open http://localhost:5173 in your browser
    2. Verify you see a login/register form (not the map or bakery list)
    3. Click "Need an account? Register" (or similar toggle)
    4. Create an account with a username and password
    5. Verify you are now logged in and can see the map, bakery list, and add form
    6. Try adding a bakery or viewing existing bakeries to confirm API calls work with auth
    7. Refresh the page (Cmd+R / F5)
    8. Verify you are STILL logged in (session restored from HttpOnly cookie)
    9. Find the "Log out" button in the nav bar and click it
    10. Verify you are back at the login form
    11. Refresh the page — verify you stay on the login form (no session)
    12. Log back in with the account you created — verify it works
  </verify>
  <done>Human confirms complete auth flow works: register, login, session persistence across refresh, logout, re-login.</done>
</task>

</tasks>

<verification>
- All backend tests pass
- Full browser auth flow works: register -> login -> use app -> refresh (still logged in) -> logout -> login again
</verification>

<success_criteria>
- Human confirms complete auth flow works in real browser
- Session persists across page refresh
- Logout clears session completely
- All existing app functionality (map, bakeries) works with auth
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-database-foundation/01-03-SUMMARY.md`
</output>
